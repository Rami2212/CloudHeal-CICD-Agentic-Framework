# Core training dependencies
transformers>=4.41.0
peft>=0.11.0
accelerate>=0.30.0
datasets>=2.19.0
pyyaml>=6.0.1
tqdm>=4.66.0
safetensors>=0.4.3
# PyTorch (ROCm install may override this)
torch>=2.1.0
